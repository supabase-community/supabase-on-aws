{
  "name": "db-initializer",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.ts --bundle --minify --sourcemap --platform=node --target=es2021 --external:pg-native --outfile=dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.131.0",
    "@databases/pg": "^5.4.1",
    "@types/aws-lambda": "^8.10.101"
  },
  "devDependencies": {
    "esbuild": "^0.14.50",
    "typescript": "^4.7.4"
  }
}
